{
  "version": "0.2.0",
  "configurations": [
  {
      "type": "node",
      "request": "launch",
      "name": "Launch Program",
      "skipFiles": [
        "<node_internals>/**"
      ],
      "cwd": "${workspaceFolder}/web-client",
      "program": "${workspaceFolder}/web-client/node_modules/.bin/jest"
    }
  ]
}
